<Window x:Class="KBSBoot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KBSBoot" 
        xmlns:view="clr-namespace:KBSBoot.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate x:Name="addMemdberAdminViewTemplate" DataType="{x:Type view:AddMemberAdminViewModel}">
            <view:AddMemberAdmin DataContext="{Binding}"></view:AddMemberAdmin>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42"/>
            <RowDefinition Height="277*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Button x:Name="addMember" Content="Lid toevoegen" HorizontalAlignment="Right" Margin="10,10,10,10" VerticalAlignment="Top" Width="92" Click="addMember_Click" Height="20"/>
        </DockPanel>

        <ContentControl Grid.Row="1" Content="{Binding}"/>
    </Grid>
</Window>
